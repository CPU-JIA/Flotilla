# Flotilla Nginx Configuration
# Domain: 571732.xyz

# 官网 - flotilla.571732.xyz
server {
    listen 80;
    server_name flotilla.571732.xyz;

    # ACME Challenge for Let's Encrypt
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect to HTTPS (uncomment after SSL setup)
    # return 301 https://$server_name$request_uri;

    # Temporary HTTP access before SSL
    location / {
        proxy_pass http://flotilla-website:3003;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# 官网 - HTTPS (启用 SSL 后取消注释)
# server {
#     listen 443 ssl http2;
#     server_name flotilla.571732.xyz;
#
#     ssl_certificate /etc/letsencrypt/live/flotilla.571732.xyz/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/flotilla.571732.xyz/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://flotilla-website:3003;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

# 应用 - app.571732.xyz
server {
    listen 80;
    server_name app.571732.xyz;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://flotilla-frontend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# 应用 - HTTPS
# server {
#     listen 443 ssl http2;
#     server_name app.571732.xyz;
#
#     ssl_certificate /etc/letsencrypt/live/app.571732.xyz/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/app.571732.xyz/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://flotilla-frontend:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }

# API - api.571732.xyz
server {
    listen 80;
    server_name api.571732.xyz;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://flotilla-backend:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # CORS headers for API
        add_header Access-Control-Allow-Origin *;
        add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS";
        add_header Access-Control-Allow-Headers "Content-Type, Authorization";
    }
}

# API - HTTPS
# server {
#     listen 443 ssl http2;
#     server_name api.571732.xyz;
#
#     ssl_certificate /etc/letsencrypt/live/api.571732.xyz/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.571732.xyz/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://flotilla-backend:4000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         add_header Access-Control-Allow-Origin *;
#         add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS";
#         add_header Access-Control-Allow-Headers "Content-Type, Authorization";
#     }
# }

# 主域名重定向到官网
server {
    listen 80;
    server_name 571732.xyz www.571732.xyz;
    return 301 http://flotilla.571732.xyz$request_uri;
}
