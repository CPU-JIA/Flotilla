# ============================================
# Flotilla Backend Environment Variables
# ============================================
# 复制此文件为 .env 并填写实际值
# cp .env.example .env
#
# ⚠️ 生成强密钥: openssl rand -base64 32
# ============================================

# ============================================
# 数据库配置 (Database Configuration)
# ============================================
DATABASE_URL="postgresql://user:password@localhost:5434/flotilla?schema=public"

# 🔒 PERFORMANCE FIX: 数据库连接池配置 (可选，有默认值)
DATABASE_CONNECTION_LIMIT=20  # 连接池大小限制 (默认: 20)
DATABASE_POOL_TIMEOUT=20      # 连接池超时时间(秒) (默认: 20)

# ============================================
# JWT 配置 (JWT Configuration)
# ============================================
# ⚠️ CRITICAL: 生产环境必须设置 ≥32 字符的强密钥！
# 生成命令: openssl rand -base64 43 (推荐使用43字符以符合HS256标准)
JWT_SECRET="your-super-secret-jwt-key-min-32-characters-change-this-in-production"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-min-32-characters-change-this"
JWT_EXPIRATION="15m"    # Access Token 有效期 (默认: 15分钟)
JWT_REFRESH_EXPIRATION="7d"  # Refresh Token 有效期 (默认: 7天)

# ============================================
# Two-Factor Authentication (双因素认证)
# ============================================
# ⚠️ CRITICAL: 用于加密存储 TOTP 密钥和恢复码 (AES-256-GCM)
# ⚠️ 至少32字符！一旦设置后不要更改，否则用户将无法访问2FA
# 生成命令: openssl rand -base64 32
TWO_FACTOR_ENCRYPTION_KEY="your-2fa-encryption-key-min-32-chars-change-in-production"

# ============================================
# 安全配置 (Security Configuration)
# ============================================
NODE_ENV="development"  # development | production | test
REQUIRE_EMAIL_VERIFICATION="false"  # 是否强制邮箱验证 (生产环境建议: true)

# 🔒 SECURITY FIX: CSRF 保护 (生产环境自动启用)
ENABLE_CSRF="true"  # 是否启用CSRF保护 (默认: true, 仅生产环境生效)

# 初始管理员邮箱 (首个注册用户自动提升为SUPER_ADMIN)
# ⚠️ CRITICAL: 生产环境必须设置！
INITIAL_ADMIN_EMAIL="admin@example.com"

# ============================================
# Webhook 配置 (Webhook Configuration)
# ============================================
# ⚠️ 可选：用于验证来自外部CI/CD系统的webhook签名 (HMAC-SHA256)
# 如果不设置，系统会为每个webhook自动生成密钥
# 生成命令: openssl rand -base64 32
WEBHOOK_SECRET=""

# ============================================
# 邮件服务配置 (Email Service Configuration)
# ============================================
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT=587
EMAIL_USER="your-email@gmail.com"
EMAIL_PASSWORD="your-app-password"
EMAIL_FROM="Flotilla <noreply@flotilla.dev>"

# ============================================
# Redis 配置 (Redis Configuration)
# ============================================
REDIS_HOST="localhost"
REDIS_PORT=6380
REDIS_PASSWORD=""  # 可选
REDIS_DB=0

# ============================================
# MinIO 配置 (Object Storage Configuration)
# ============================================
MINIO_ENDPOINT="localhost"
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY="minioadmin"
MINIO_SECRET_KEY="minioadmin"
MINIO_BUCKET="flotilla"

# ============================================
# MeiliSearch 配置 (Search Engine Configuration)
# ============================================
MEILISEARCH_HOST="http://localhost:7700"
MEILISEARCH_API_KEY=""  # 可选

# ============================================
# CORS 配置 (CORS Configuration)
# ============================================
# 方式1: 多域名配置 (生产环境推荐)
CORS_ALLOWED_ORIGINS="https://your-frontend.com,https://www.your-frontend.com"

# 方式2: 单独配置 (开发环境)
FRONTEND_URL="http://localhost:3000"
WEBSITE_URL="http://localhost:3003"

# ============================================
# 应用配置 (Application Configuration)
# ============================================
PORT=4000  # 后端服务端口

# ============================================
# Git 存储配置 (Git Storage Configuration)
# ============================================
GIT_STORAGE_PATH="./repos"  # Git 仓库存储路径 (默认: ./repos)

# ============================================
# OAuth/SSO 配置 (OAuth/SSO Configuration)
# ============================================
# GitHub OAuth - https://github.com/settings/developers
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""
GITHUB_CALLBACK_URL="http://localhost:4000/auth/oauth/github/callback"

# Google OAuth - https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_CALLBACK_URL="http://localhost:4000/auth/oauth/google/callback"

# Enterprise SAML SSO (预留配置)
SAML_ENTRY_POINT=""
SAML_ISSUER=""
SAML_CERT=""
SAML_CALLBACK_URL="http://localhost:4000/auth/oauth/saml/callback"

# ============================================
# SMTP 邮件配置 (SMTP Email Configuration)
# ============================================
# 推荐服务商:
#   - Brevo: https://www.brevo.com/ (免费额度: 9000封/月)
#   - SendGrid: https://sendgrid.com/ (免费额度: 100封/天)
#   - Mailgun: https://www.mailgun.com/ (免费额度: 5000封/月)
SMTP_HOST="smtp.your-provider.com"
SMTP_PORT=587  # 587 for STARTTLS, 465 for SSL/TLS
SMTP_SECURE=""  # 空值表示使用STARTTLS (587端口), "true"表示SSL (465端口)
SMTP_USER="your-smtp-username@domain.com"
SMTP_PASS="YOUR_SMTP_PASSWORD_OR_API_KEY"
SMTP_FROM_EMAIL="noreply@your-domain.com"
SMTP_FROM_NAME="Flotilla Team"

# ============================================
# 测试环境配置 (Testing Configuration)
# ============================================
# 仅测试环境使用
SEED_ADMIN_EMAIL="admin@test.com"  # 种子管理员邮箱
SEED_ADMIN_PASSWORD="AdminPass123!"  # 种子管理员密码
